node_modules
dist

# Generated by demo/agent runs, not part of the store's data
data/audit-log.jsonl
data/test-audit-log.jsonl

# Stripe test keys and other secrets
.env
.env.*
!.env.example

# Vite build output (run: npm run web:build)
web/dist

# React app deps
web/node_modules

# TypeScript incremental build info
*.tsbuildinfo
